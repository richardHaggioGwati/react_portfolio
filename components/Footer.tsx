import { Box } from '@mui/material';

const Footer = () => {
	return (
        <Box display='flex' flex={12} bgcolor='red' sx={{
            marginTop: '1rem',
        }}>
			<p>Footer</p>
		</Box>
	);
};

export default Footer;
